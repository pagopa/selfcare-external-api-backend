meta {
  name: getDocumentByPathVariants
  type: http
  seq: 12
}

// This file was used to experiment with encoded vs decoded documentPath variants.
// The experiments were split into `getDocumentByPathEncoded.bru` and
// `getDocumentByPathDecoded.bru`. Keep this file as a placeholder to avoid
// parser errors in CI.

settings {
  encodeUrl: true
}
